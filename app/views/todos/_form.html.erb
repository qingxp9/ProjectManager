<%= form_for([@project, @project.todos.build]) do |f| %>
  <p>
    <%= f.label :goal %><br>
    <%= f.text_field :goal %>
  </p>
  <p>
    <%= f.label :team_member_id %><br>
    <%= f.text_area :team_member_id %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
